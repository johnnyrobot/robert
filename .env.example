# Firebase Configuration (Optional - for user authentication)
# Get this from Firebase Console > Project Settings > General > Web API Key
FIREBASE_API_KEY=

# Note: Canvas API tokens and OpenRouter keys are entered by users
# in the web interface and stored only in session memory (never persisted)
